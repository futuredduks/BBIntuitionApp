using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ResultCalendarManager : MonoBehaviour
{

    public CalendarUI resultCalendar;
    // Start is called before the first frame update
    void Start()
    {
        resultCalendar.Init();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
